type NFTMinted @entity {
  id: ID!
  tokenId: BigInt! # uint256
  owner: String! # address
  sender: String!
  # owner: User! # address
  # sender: User!
}

# type User @entity {
#   id: ID!
#   nfts: [NFTMinted!]! @derivedFrom(field: "sender")
# }
